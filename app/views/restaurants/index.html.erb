<h1>All Restaurants availables</h1>
<div>
  <ul>
    <% @restaurants.each do |restaurant| %>
      <li>
        <%= link_to restaurant.name, restaurant_path(restaurant) %>
      </li>
    <% end %>
  </ul>
</div>
<%= button_to 'Add a new restaurant', new_restaurant_path, method: :get%>
